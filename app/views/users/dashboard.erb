<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>
<body>
    <h1>Welcome, <%= @user.name.capitalize %>! Let's start planting.</h1>
    <% if @user.plants.empty? %>
            <p><%= "There are no plants to display!" %></p>
    <% else %>
        <h2>Your Plants:</h2>
        <ul>
            <% @user.plants.each do |plant| %>
                <h3><a href="/plants/<%= plant.id %>"><%= plant.name %></a></h3>
                
                <br><br>
            <% end %>
        </ul>

    <% end %>

    
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>
<body>
    <h1>Welcome, <%= @user.name.capitalize %>! Let's start planting!</h1>
    <% if @user.plants.empty? %>
            <p><%= "There are no plants to display!" %></p>
    <% end %>

    <ul>
        <% @user.plants.each do |plant| %>
        <h3><%= plant.name.capitalize %></h3>
        <li>Description: <%= plant.description %></li>
        <li>Amount: <%= plant.how_many %></li>            <li>Planting Schedule: <%= plant.planting_schedule %></li>
        <li>Harvest: <%= plant.harvest %></li>
        <li>Location: <%= plant.location %></li>
        <% end %>
    </ul>

    <form action='/dashboard' method='post'></form>
    <a href='/plants/new'><h3>Add a new plant</h3></a>
    <a href='/plants'><h3>View all plants</h3></a>
    <a href='/plants/edit'><h3>Edit an existing plant</h3></a>
    <a href='/users/<%=@user.id%>/edit'><h3>Edit your profile</h3></a>
    <a href='/plants/<%=@user.id%>'><h3>Delete a plant</h3></a>
    
    <form action='/logout' method='POST'>
        <input type='hidden' name='_method' value='delete'>
        <input type='submit' name='submit' value='Logout'>
    </form>
    
</body>
</html>
